---
name: xh-skills-video-insight
description: >
  从公网URL分析视频内容。提取语音为SRT字幕，逐帧分析视觉画面，生成综合视频理解报告（含分段详解）和纯文字内容详情文章。当用户要求分析视频、理解视频内容、提取字幕、生成视频报告、视频转文字、视频摘要时使用。触发短语包括：分析视频、提取字幕、视频理解、视频报告、视频转文字、视频总结、analyze
  video、generate subtitles、video understanding、video to text。需要设置DASHSCOPE_API_KEY环境变量用于阿里云百炼API访问。
---

# 视频洞察 Skill

分析视频内容并产出四个文件：

1. **SRT字幕** — 带时间戳的完整语音转录
2. **对齐数据** — 时间同步的语音+画面描述（中间产物）
3. **视频理解报告** — 综合语音和视觉内容的详细分析
4. **内容详情文章** — 将视频改写为纯文字知识文章

## 架构

整体流程分为两个阶段：Python脚本负责数据采集，Claude负责报告生成。

- **阶段1 — 语音识别**：Fun-ASR-MTL（`fun-asr-mtl`），通过DashScope异步API，将视频语音转为带词级时间戳的文字
- **阶段2 — 视觉分析**：Qwen3.6-Plus（`qwen3.6-plus`），通过OpenAI兼容API，逐帧分析画面生成带时间戳的场景描述
- **阶段3 — 报告生成**：Claude读取对齐数据，按本Skill指令生成视频理解报告和内容详情

## 前置条件

1. 阿里云百炼API密钥，设置为环境变量 `DASHSCOPE_API_KEY`
2. Python 3.9+，安装依赖：

   ```
   pip install -r "{baseDir}/requirements.txt"
   ```

**依赖**：python>=3.9, dashscope>=1.24.6, openai>=1.52.0

## 执行流程

### 第1步：运行数据采集脚本

执行Python脚本采集语音识别和视觉分析数据：

```bash
python3 "{baseDir}/scripts/collect_data.py" \
  --url "<视频公网URL>" \
  --output <输出目录>
```

可选参数：

- `--fps <值>` — 视觉分析的帧提取率，默认1.0，范围0.1-10。对快节奏视频可用更高值（2-5）以获得更精细的场景检测。
- `--lang <语言代码...>` — 语音识别的语言提示，默认 `zh en`。支持：zh、en、ja、ko、fr、de、es、pt等。

脚本会在输出目录生成以下文件：

- `subtitles.srt` — 标准SRT字幕文件
- `aligned_data.md` — 时间对齐的语音+画面描述数据
- `pipeline_debug.json` — 完整的中间数据，供调试使用

**注意**：视频必须可通过公网URL访问。Fun-ASR-MTL为异步处理，脚本会自动轮询等待结果，通常耗时10-60秒。

### 第2步：读取对齐数据

读取输出目录中的 `aligned_data.md`。该文件包含时间同步的语音转录和视觉场景描述，格式如下：

```
### [HH:MM:SS - HH:MM:SS]
**语音**：转录的语音文本
**画面**：场景的视觉描述
```

### 第3步：生成视频理解报告

根据对齐数据，生成 `video_insight_report.md`。严格遵循以下结构：

```
# 视频理解报告

## 视频概要
- **视频主题**：[一句话概括]
- **视频时长**：[估算的总时长]
- **内容类型**：[教学/演讲/娱乐/新闻/Vlog/产品介绍/其他]
- **语言**：[使用的语言]
- **整体风格**：[正式/轻松/专业/生活化等]

## 分段详解
（将视频按内容逻辑分成若干段落，每段包含以下信息）

### 段落 N：段落标题（[开始时间 - 结束时间]）
- **画面内容**：该时间段画面的整体描述
- **语音内容**：该时间段说话内容的概括（提炼要点，不必逐字复述）
- **综合理解**：结合画面和语音，说明这一段在表达什么，核心信息是什么
- **关键细节**：值得关注的画面细节、语气变化、场景切换等

## 关键节点
（列出视频中最重要的3-8个关键时刻，以表格形式呈现）

| 时间戳 | 节点描述 | 重要性说明 |
|--------|---------|-----------|
| HH:MM:SS | 这个时刻发生了什么 | 为什么这是关键节点 |
```

**分析原则**：

- 将语音和画面在时间轴上对齐——同一时间窗口里，说了什么、画面展现了什么，结合起来理解
- 如果语音和画面存在呼应关系（比如讲解内容在解释画面中的事物），请明确指出
- 对于没有语音的时间段，仅根据画面内容进行描述
- 描述要具体、有细节，避免空洞和泛泛而谈

### 第4步：生成内容详情文章

根据对齐数据，生成 `content_detail.md`。这是一篇纯文字的知识文章，将视频内容完整改写，不带有任何视频格式的痕迹。遵循以下规则：

1. **纯文字表达**：文章中绝对不能出现"画面"、"视频"、"时间戳"、"镜头"、"屏幕"、"帧"、"片段"等与视频格式相关的词汇。写作时应当假设读者根本不知道这来源于一段视频，就像在读一篇独立的知识文章。

2. **高度还原**：尽可能详细地还原视频中传达的所有知识点、观点、案例、数据和推理过程。不要遗漏重要信息。数字、公式、评分标准等具体数据要原样保留。对于案例部分（如隔离器的DFMEA/PFMEA分析），要完整再现每个字段的值和含义。

3. **灵活运用格式增强可读性**：
   - 当涉及对比性内容时（如DFMEA vs PFMEA），优先使用**表格**呈现，让读者一目了然
   - 当罗列要点、步骤、要素时，使用**列表**清晰呈现
   - 对于公式、关键定义、重要结论，使用**引用块**（`>`）突出展示
   - 示例数据（如RPN计算过程）用表格逐字段展示最为清晰
   - 适当使用加粗强调核心概念和关键术语

4. **结构清晰**：按照以下结构组织文章：
   - 第一部分：主题概述（对应视频概要，让读者快速了解这篇文章讲什么）
   - 第二部分：详细内容（对应分段详解，按内容的逻辑层次展开，分段标题中不出现时间信息，而是基于知识脉络来切分章节）
   - 第三部分：核心要点（对应关键节点，总结最核心的几个观点，不含时间信息）

5. **语言风格**：以视频中的主要语言为准，保持专业准确的同时兼顾可读性。对专业术语在首次出现时给出全称。让文章既经得起专业读者的审视，也便于初学者理解。

6. **文章标题**使用 `# 内容详情`。

### 第5步：向用户报告结果

生成所有文件后，向用户报告：

- 三个输出文件及其路径
- 视频内容的简要概括（2-3句话）
- 主动提出可以展示或详细说明报告中任何部分

## 输出文件一览

| 文件 | 说明 |
|------|------|
| `subtitles.srt` | 标准SRT字幕文件，带时间戳 |
| `aligned_data.md` | 时间对齐的语音+画面数据（中间产物） |
| `video_insight_report.md` | 综合视频分析报告，含分段详解和关键节点表格 |
| `content_detail.md` | 纯文字知识文章，无视频格式痕迹 |
| `pipeline_debug.json` | 完整的中间数据，供调试使用 |

## 常见问题与错误处理

| 情况 | 处理方式 |
|------|---------|
| `DASHSCOPE_API_KEY` 未设置 | 提示用户设置：`export DASHSCOPE_API_KEY="sk-..."`，或写入 `~/.zshrc` 持久化 |
| ASR识别超时（超过10分钟） | 视频音频可能过长，建议使用较短的片段重试 |
| ASR返回空结果（无句子） | 检查视频是否有音轨，或尝试调整语言提示（`--lang` 参数） |
| 视觉分析无返回场景 | 检查视频URL是否可公网访问，确认视频格式被支持（MP4等常见格式） |
| 视觉分析JSON解析失败 | 脚本会降级为将原始文本作为单个场景返回，不会中断流程 |
| `pip install` 失败（PEP 668） | 尝试 `pip install --break-system-packages` 或使用虚拟环境 |
| 视频URL不可达 | 用 `curl -I <URL>` 确认可访问性，检查是否需要认证或特定网络环境 |
| 对齐数据中语音和画面时间窗口不匹配 | 脚本采用重叠检测算法，部分场景可能无匹配语音（属正常情况，用"无语音"标注） |
| 输出目录已存在旧结果 | 脚本会直接覆盖同名文件，不会自动备份 |

## 补充说明

- 脚本会自动处理ASR异步轮询，典型等待时间10-60秒
- 视觉分析默认 `fps=1.0`（每秒一帧），兼顾覆盖度和API成本
- 所有中间数据保存在 `pipeline_debug.json`，出现问题时可据此排查
- `aligned_data.md` 是脚本和Claude报告生成之间的桥梁，包含生成报告和内容详情所需的全部信息
- 视频长度建议控制在30分钟以内，过长的视频可能触发API超时或上下文溢出
